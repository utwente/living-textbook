<nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="{{ path('app_default_dashboard') }}">Living Textbook</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_default_dashboard') }}">{{ 'app.dashboard'|trans }}</a>
      </li>
    </ul>

    <a class="btn btn-outline-primary ml-lg-2" href="#" onclick="eDispatch.toggleConceptBrowser(); $(this).blur();">{{ 'browser.toggle'|trans }}</a>

    {% if app.user %}
      <a class="btn btn-outline-dark ml-lg-2" class="top-level" href="{{ path('logout') }}">{{ 'login.logout'|trans }}</a>
    {% else %}
      <a class="btn btn-outline-primary ml-lg-2" class="top-level" href="{{ path('login') }}">{{ 'login.login'|trans }}</a>
    {% endif %}
  </div>
</nav>
